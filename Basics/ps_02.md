<!-- 
    Pseudocode for finding minimum value

    PROGRAM Min_Value
        PRINT "Enter a number"
        READ num1
        PRINT "Enter another number"
        READ num2
        IF num1 < num2
            PRINT num1 "is less than" num2
        ELSE IF num1 > num2
            PRINT num2 "is less than" num1
        ELSE 
            PRINT "Both numbers are equal"
        END IF
    END PROGRAM

    NOTE:-
    use of END IF is necessary  to end the IF statement

 -->